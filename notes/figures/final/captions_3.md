# Figure captions - example events (Methods, phase picking)

Generated by `scripts/72_example_waveforms.py`. Counts are recomputed from the pick pools at build time.

**G16 - Example events: raw picker output, associated picks and analyst picks (overview).** Vertical-component record sections of four representative earthquakes, nearest 6 stations each, 3-20 Hz zero-phase (`src/bransfield_eq/xcfilter.py`), traces sorted by epicentral distance (station code and distance in km on the left axis) and individually normalised. Time is relative to the NLLoc v6 origin. Faded dotted ticks are every pick the two production pickers (PhaseNet-DiTing, PickBlue-PhaseNetLight) emitted inside the window, with no probability cut - P drawn above the trace, S below; solid blue/orange ticks are the subset pyocto associated to the event; black dashed ticks are the analyst's manual picks; thin grey ticks are the arrival times predicted by the final NLLoc v6 location. The compact panels use their own window (1 s before the first P and 2 s after the last S of the traces shown). Across the four events 161 raw picks fall in the plotted windows and 78 of them are associated to the event - the rest are the pickers' doubles of the same arrival (both pickers run on every station), picks of other phases, and false or unassociated detections. Panels (a)-(d) are shown full size, with the horizontal component and the S picks, in G16a-G16d.

**G16a - shallow caldera event (event 82625).** 2019-10-13 00:19:05 UTC, -62.4251/-58.4060, 2.51 km below sea level (1.41 km below the local seafloor), Nphs 32, rms 0.172 s, sigma_z 0.21 km, azimuthal gap 41 deg. Left: vertical component (ELZ or HHZ) with the P layers; right: the horizontal (SL1/SL2 or HH1/HH2) with the larger S amplitude, with the S layers; the channel actually plotted is printed above each trace. Markers as in G16. 48 raw picker picks fall in the 5.9 s window on the 14 traces shown (PhaseNet-DiTing 26 / PickBlue-PhaseNetLight 22); pyocto associated 26 of them to this event (14 P, 12 S) out of the 32 phases it associated in total, and the analyst picked 21 arrivals on this event. The 18 arrivals picked both automatically and by hand differ by a median of 45 ms (largest 476 ms). 3 associated station(s) beyond 25 km are not plotted (5M.FER at 37.3 km, ZX.BRA10 at 40.3 km, 5M.OHI at 103.0 km), so the near traces are not squeezed into the first seconds of a long axis. Vermillion labels give the associated-minus-manual difference in ms where both exist. NLLoc predicted arrivals are from `nlloc/output/year_v6/shard_11/loc.20191013.001906.grid0.loc.hyp` (32 phases).

**G16b - deeper event under the array (event 30153).** 2019-03-17 10:51:59 UTC, -62.4222/-58.4085, 5.49 km below sea level (4.39 km below the local seafloor), Nphs 24, rms 0.220 s, sigma_z 0.56 km, azimuthal gap 66 deg. Left: vertical component (ELZ or HHZ) with the P layers; right: the horizontal (SL1/SL2 or HH1/HH2) with the larger S amplitude, with the S layers; the channel actually plotted is printed above each trace. Markers as in G16. 49 raw picker picks fall in the 5.2 s window on the 13 traces shown (PhaseNet-DiTing 26 / PickBlue-PhaseNetLight 23); pyocto associated 21 of them to this event (11 P, 10 S) out of the 24 phases it associated in total, and the analyst picked 26 arrivals on this event. The 21 arrivals picked both automatically and by hand differ by a median of 30 ms (largest 306 ms). 3 associated station(s) beyond 25 km are not plotted (ZX.BRA08 at 33.1 km, ZX.BRA10 at 40.5 km, ZX.BRA02 at 61.3 km), so the near traces are not squeezed into the first seconds of a long axis. Vermillion labels give the associated-minus-manual difference in ms where both exist. NLLoc predicted arrivals are from `nlloc/output/year_v6/shard_07/loc.20190317.105200.grid0.loc.hyp` (24 phases).

**G16c - small event with few phases (event 66766).** 2019-07-03 00:02:03 UTC, -62.4175/-58.3937, 4.79 km below sea level (3.89 km below the local seafloor), Nphs 8, rms 0.167 s, sigma_z 0.78 km, azimuthal gap 102 deg. Left: vertical component (ELZ or HHZ) with the P layers; right: the horizontal (SL1/SL2 or HH1/HH2) with the larger S amplitude, with the S layers; the channel actually plotted is printed above each trace. Markers as in G16. 13 raw picker picks fall in the 4.2 s window on the 5 traces shown (PhaseNet-DiTing 7 / PickBlue-PhaseNetLight 6); pyocto associated 8 of them to this event (3 P, 5 S) out of the 8 phases it associated in total, and the analyst picked 8 arrivals on this event. The 7 arrivals picked both automatically and by hand differ by a median of 35 ms (largest 120 ms). Vermillion labels give the associated-minus-manual difference in ms where both exist. NLLoc predicted arrivals are from `nlloc/output/year_v6/shard_13/loc.20190703.000205.grid0.loc.hyp` (8 phases).

**G16d - event in the analyst-trusted window (event 87517).** 2019-12-03 13:21:03 UTC, -62.4215/-58.4003, 2.66 km below sea level (1.48 km below the local seafloor), Nphs 24, rms 0.144 s, sigma_z 0.31 km, azimuthal gap 66 deg. Left: vertical component (ELZ or HHZ) with the P layers; right: the horizontal (SL1/SL2 or HH1/HH2) with the larger S amplitude, with the S layers; the channel actually plotted is printed above each trace. Markers as in G16. 51 raw picker picks fall in the 7.2 s window on the 14 traces shown (PhaseNet-DiTing 25 / PickBlue-PhaseNetLight 26); pyocto associated 23 of them to this event (12 P, 11 S) out of the 24 phases it associated in total, and the analyst picked 23 arrivals on this event. The 20 arrivals picked both automatically and by hand differ by a median of 46 ms (largest 195 ms). Vermillion labels give the associated-minus-manual difference in ms where both exist. NLLoc predicted arrivals are from `nlloc/output/year_v6/shard_15/loc.20191203.132103.grid0.loc.hyp` (24 phases).
